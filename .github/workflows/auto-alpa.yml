name: Auto Simpan Presensi Tidak Hadir (Alpa 15:30 WIB)

on:
  schedule:
    # 08:30 UTC = 15:30 WIB (Senin s.d. Jumat)
    - cron: '30 8 * * 1-5'
  # Memungkinkan eksekusi manual via tombol 'Run workflow' di tab Actions GitHub
  workflow_dispatch:

jobs:
  auto-alpa:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🤖 Jalankan Auto-Alpa Script (15:30 WIB)
        run: node scripts/auto-alpa.js
